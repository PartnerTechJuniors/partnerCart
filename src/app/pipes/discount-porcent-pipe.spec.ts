import { DiscountPorcentPipe } from './discount-porcent-pipe';

describe('DiscountPorcentPipe', () => {
  it('create an instance', () => {
    const pipe = new DiscountPorcentPipe();
    expect(pipe).toBeTruthy();
  });
});
